{
  "components": {
    "schemas": {
      "AuthCapabilitiesResponse": {
        "additionalProperties": false,
        "description": "GET /api/auth/capabilities — feature-activation discovery for auth-related, environment-gated affordances (AC-10). googleLoginEnabled gates the web Google-login affordance; googleLoginMobileEnabled gates the mobile one — independent env configurations.",
        "properties": {
          "googleLoginEnabled": {
            "description": "true when GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET are both configured (container.googleAuth !== undefined), false otherwise (AUTH-16).",
            "type": "boolean"
          },
          "googleLoginMobileEnabled": {
            "description": "true when GOOGLE_CLIENT_ID_ANDROID is configured (container.googleAuthMobile !== undefined), false otherwise (AUTH-22). Computed independently of googleLoginEnabled — either, both, or neither may be true.",
            "type": "boolean"
          }
        },
        "required": [
          "googleLoginEnabled",
          "googleLoginMobileEnabled"
        ],
        "type": "object"
      },
      "AuthLoginResponse": {
        "additionalProperties": false,
        "description": "POST /api/auth/login — successful authentication (AUTH-01).",
        "properties": {
          "expiresAt": {
            "description": "ISO-8601 UTC timestamp.",
            "type": "string"
          },
          "token": {
            "type": "string"
          },
          "userId": {
            "type": "string"
          }
        },
        "required": [
          "token",
          "userId",
          "expiresAt"
        ],
        "type": "object"
      },
      "AuthMeResponse": {
        "additionalProperties": false,
        "description": "GET /api/auth/me and PATCH /api/perfil — the authenticated user identity, including whether Google is linked (AUTH-09, US-040, VINC041-08).",
        "properties": {
          "email": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "null only for esDemo=true (demo) accounts — a domain invariant, not enforced by this schema."
          },
          "esDemo": {
            "type": "boolean"
          },
          "googleVinculado": {
            "description": "VINC041-08. Whether the account has a linked Google identity, derived from googleSub !== null. The raw googleSub never crosses this boundary.",
            "type": "boolean"
          },
          "nombre": {
            "type": "string"
          },
          "userId": {
            "type": "string"
          }
        },
        "required": [
          "userId",
          "nombre",
          "esDemo",
          "email",
          "googleVinculado"
        ],
        "type": "object"
      },
      "BucketDetalleMesResponse": {
        "additionalProperties": false,
        "description": "GET /api/buckets/:bucket/detalle — month×bucket detail grouped by category: header totals, % vs meta, and category groups with ALL their transactions (US-051).",
        "properties": {
          "bucket": {
            "description": "Validated bucket name (echo, not raw input) — one of the 4-bucket allowlist (D-08).",
            "type": "string"
          },
          "grupos": {
            "description": "One entry per present category, es-CL alphabetical, \"Sin categoría\" last. [] for an empty bucket month (MBD-01).",
            "items": {
              "additionalProperties": false,
              "properties": {
                "categoriaId": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "description": "null for the synthetic group."
                },
                "conteo": {
                  "maximum": 9007199254740991,
                  "minimum": 0,
                  "type": "integer"
                },
                "nombre": {
                  "type": "string"
                },
                "subtotal": {
                  "description": "BigInt-safe decimal string amount (never a JSON number).",
                  "type": "string"
                },
                "transacciones": {
                  "description": "Complete list — never truncated or paged (MBD-02).",
                  "items": {
                    "additionalProperties": false,
                    "properties": {
                      "descripcion": {
                        "type": "string"
                      },
                      "fecha": {
                        "description": "ISO-8601 UTC timestamp.",
                        "type": "string"
                      },
                      "id": {
                        "type": "string"
                      },
                      "monto": {
                        "description": "BigInt-safe decimal string amount (never a JSON number).",
                        "type": "string"
                      },
                      "origen": {
                        "description": "Bank name verbatim, or 'Manual' for a hand-entered movement (D-02). Drives the delete affordance on manual rows (WEB-DEL-01).",
                        "type": "string"
                      }
                    },
                    "required": [
                      "id",
                      "fecha",
                      "descripcion",
                      "origen",
                      "monto"
                    ],
                    "type": "object"
                  },
                  "type": "array"
                }
              },
              "required": [
                "categoriaId",
                "nombre",
                "subtotal",
                "conteo",
                "transacciones"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "metaBp": {
            "anyOf": [
              {
                "maximum": 9007199254740991,
                "minimum": -9007199254740991,
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Bucket's 50/30/20 target from BANDAS_SEMAFORO; null when absent (D-05)."
          },
          "periodo": {
            "description": "Resolved period, format YYYY-MM.",
            "type": "string"
          },
          "porcentajeBp": {
            "anyOf": [
              {
                "maximum": 9007199254740991,
                "minimum": -9007199254740991,
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Basis-point percentage, round-half-up. null when the bucket has no meta rule (SinCategoria) or the month has no income (D-05)."
          },
          "total": {
            "description": "BigInt-safe decimal string amount (never a JSON number).",
            "type": "string"
          },
          "totalCategorias": {
            "description": "Includes the synthetic Sin categoría group when present (D-09).",
            "maximum": 9007199254740991,
            "minimum": 0,
            "type": "integer"
          },
          "totalTransacciones": {
            "maximum": 9007199254740991,
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "periodo",
          "bucket",
          "total",
          "totalTransacciones",
          "totalCategorias",
          "porcentajeBp",
          "metaBp",
          "grupos"
        ],
        "type": "object"
      },
      "CatalogoErrorResponse": {
        "additionalProperties": false,
        "description": "Error body for the 4 new catalog endpoints (US-038). Not retrofitted onto pre-existing operations. `indice` is present only for a nested-patrón validation failure on POST /api/categorias (CAT038-11).",
        "properties": {
          "code": {
            "type": "string"
          },
          "indice": {
            "type": "number"
          },
          "message": {
            "type": "string"
          }
        },
        "required": [
          "message",
          "code"
        ],
        "type": "object"
      },
      "CatalogoResponse": {
        "additionalProperties": false,
        "description": "GET /api/categorias — the authenticated caller's full catalog (US-038, CAT038-02).",
        "properties": {
          "categorias": {
            "items": {
              "$ref": "#/components/schemas/CategoriaResponse"
            },
            "type": "array"
          }
        },
        "required": [
          "categorias"
        ],
        "type": "object"
      },
      "CategoriaResponse": {
        "additionalProperties": false,
        "description": "A category with its nested classification patterns and the all-history count of the caller's own referencing transactions (US-038, US-039 CAT039-01).",
        "properties": {
          "bucket": {
            "type": "string"
          },
          "id": {
            "type": "string"
          },
          "nombre": {
            "type": "string"
          },
          "patrones": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "categoriaId": {
                  "type": "string"
                },
                "id": {
                  "type": "string"
                },
                "matchType": {
                  "type": "string"
                },
                "patron": {
                  "type": "string"
                },
                "prioridad": {
                  "type": "number"
                }
              },
              "required": [
                "id",
                "categoriaId",
                "patron",
                "matchType",
                "prioridad"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "transaccionesCount": {
            "type": "number"
          }
        },
        "required": [
          "id",
          "nombre",
          "bucket",
          "patrones",
          "transaccionesCount"
        ],
        "type": "object"
      },
      "CommitIngestaResponse": {
        "additionalProperties": false,
        "description": "POST /api/ingestas/commit — commit result with per-row bucket + categoriaId (US-057, CMT-05).",
        "properties": {
          "duplicadosOmitidos": {
            "description": "New duplicates found at commit-time dedup — reported, never aborts (D-13/CA-03).",
            "maximum": 9007199254740991,
            "minimum": -9007199254740991,
            "type": "integer"
          },
          "ingestaId": {
            "type": "string"
          },
          "totalTransacciones": {
            "description": "Rows actually persisted (excludes duplicates omitted at commit). Not money — plain JSON number.",
            "maximum": 9007199254740991,
            "minimum": -9007199254740991,
            "type": "integer"
          },
          "transacciones": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "abono": {
                  "description": "BigInt-safe decimal string amount (never a JSON number).",
                  "type": "string"
                },
                "bucket": {
                  "description": "Serialized Bucket enum value (Necesidades|Deseos|Ahorro|Ingreso|SinCategoria). Always present for commit rows — classification is resolved pre-persist (D-11).",
                  "type": "string"
                },
                "cargo": {
                  "description": "BigInt-safe decimal string amount (never a JSON number).",
                  "type": "string"
                },
                "categoriaId": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "description": "Final category assigned, or null for Ingreso rows and DES-CLASIFICAR entries (D-11)."
                },
                "descripcion": {
                  "type": "string"
                },
                "fecha": {
                  "description": "ISO-8601 UTC timestamp.",
                  "type": "string"
                }
              },
              "required": [
                "fecha",
                "descripcion",
                "cargo",
                "abono",
                "bucket",
                "categoriaId"
              ],
              "type": "object"
            },
            "type": "array"
          }
        },
        "required": [
          "ingestaId",
          "totalTransacciones",
          "duplicadosOmitidos",
          "transacciones"
        ],
        "type": "object"
      },
      "DetalleBucketResponse": {
        "additionalProperties": false,
        "description": "GET /api/buckets/:bucket — bucket drill-down (US-017).",
        "properties": {
          "bucket": {
            "description": "Validated bucket name (echo, not raw input).",
            "type": "string"
          },
          "periodo": {
            "description": "Resolved period, format YYYY-MM.",
            "type": "string"
          },
          "transacciones": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "abono": {
                  "description": "BigInt-safe decimal string amount (never a JSON number).",
                  "type": "string"
                },
                "banco": {
                  "type": "string"
                },
                "cargo": {
                  "description": "BigInt-safe decimal string amount (never a JSON number).",
                  "type": "string"
                },
                "categoria": {
                  "anyOf": [
                    {
                      "additionalProperties": false,
                      "properties": {
                        "id": {
                          "type": "string"
                        },
                        "nombre": {
                          "type": "string"
                        }
                      },
                      "required": [
                        "id",
                        "nombre"
                      ],
                      "type": "object"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "description": "Folded category, or null for Ingreso/SinCategoria rows."
                },
                "descripcion": {
                  "type": "string"
                },
                "fecha": {
                  "description": "ISO-8601 UTC timestamp.",
                  "type": "string"
                },
                "id": {
                  "type": "string"
                },
                "numeroCuenta": {
                  "type": "string"
                },
                "tipoCuenta": {
                  "type": "string"
                }
              },
              "required": [
                "id",
                "fecha",
                "descripcion",
                "cargo",
                "abono",
                "banco",
                "tipoCuenta",
                "numeroCuenta",
                "categoria"
              ],
              "type": "object"
            },
            "type": "array"
          }
        },
        "required": [
          "periodo",
          "bucket",
          "transacciones"
        ],
        "type": "object"
      },
      "IngestasListResponse": {
        "additionalProperties": false,
        "description": "GET /api/ingestas — per-user ingesta list (US-004/US-018).",
        "properties": {
          "ingestas": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "banco": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "description": "null when an early FALLIDA ingesta never resolved a bank."
                },
                "estado": {
                  "enum": [
                    "PROCESADA",
                    "FALLIDA"
                  ],
                  "type": "string"
                },
                "fecha": {
                  "description": "ISO-8601 UTC timestamp.",
                  "type": "string"
                },
                "id": {
                  "type": "string"
                },
                "motivoFallo": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "nombreArchivo": {
                  "type": "string"
                },
                "totalTransacciones": {
                  "description": "Row count, not money — plain JSON number.",
                  "maximum": 9007199254740991,
                  "minimum": -9007199254740991,
                  "type": "integer"
                }
              },
              "required": [
                "id",
                "banco",
                "nombreArchivo",
                "estado",
                "motivoFallo",
                "fecha",
                "totalTransacciones"
              ],
              "type": "object"
            },
            "type": "array"
          }
        },
        "required": [
          "ingestas"
        ],
        "type": "object"
      },
      "IngestaUploadResponse": {
        "additionalProperties": false,
        "description": "POST /api/ingestas — successful upload result (US-004/US-005/US-011).",
        "properties": {
          "archivo": {
            "additionalProperties": false,
            "properties": {
              "extension": {
                "type": "string"
              },
              "nombre": {
                "type": "string"
              },
              "tamanoBytes": {
                "maximum": 9007199254740991,
                "minimum": -9007199254740991,
                "type": "integer"
              }
            },
            "required": [
              "nombre",
              "extension",
              "tamanoBytes"
            ],
            "type": "object"
          },
          "banco": {
            "type": "string"
          },
          "duplicadosOmitidos": {
            "description": "Count of rows skipped as duplicates. Never omitted (CA-04).",
            "maximum": 9007199254740991,
            "minimum": -9007199254740991,
            "type": "integer"
          },
          "ingestaId": {
            "type": "string"
          },
          "numeroCuenta": {
            "type": "string"
          },
          "tipoCuenta": {
            "type": "string"
          },
          "totalTransacciones": {
            "description": "Row count, not money — plain JSON number.",
            "maximum": 9007199254740991,
            "minimum": -9007199254740991,
            "type": "integer"
          },
          "transacciones": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "abono": {
                  "description": "BigInt-safe decimal string amount (never a JSON number).",
                  "type": "string"
                },
                "cargo": {
                  "description": "BigInt-safe decimal string amount (never a JSON number).",
                  "type": "string"
                },
                "descripcion": {
                  "type": "string"
                },
                "fecha": {
                  "description": "ISO-8601 UTC timestamp.",
                  "type": "string"
                }
              },
              "required": [
                "fecha",
                "descripcion",
                "cargo",
                "abono"
              ],
              "type": "object"
            },
            "type": "array"
          }
        },
        "required": [
          "ingestaId",
          "banco",
          "tipoCuenta",
          "numeroCuenta",
          "archivo",
          "totalTransacciones",
          "duplicadosOmitidos",
          "transacciones"
        ],
        "type": "object"
      },
      "IngresosMesResponse": {
        "additionalProperties": false,
        "description": "GET /api/ingresos/mes — monthly income list by origin (bank/Manual): header total, count, and ALL transactions (US-052).",
        "properties": {
          "conteo": {
            "maximum": 9007199254740991,
            "minimum": 0,
            "type": "integer"
          },
          "total": {
            "description": "BigInt-safe decimal string amount (never a JSON number).",
            "type": "string"
          },
          "transacciones": {
            "description": "Complete list — never truncated or paged (MID-01).",
            "items": {
              "additionalProperties": false,
              "properties": {
                "descripcion": {
                  "type": "string"
                },
                "fecha": {
                  "description": "ISO-8601 UTC timestamp.",
                  "type": "string"
                },
                "id": {
                  "type": "string"
                },
                "monto": {
                  "description": "BigInt-safe decimal string amount (never a JSON number).",
                  "type": "string"
                },
                "origen": {
                  "description": "Bank name verbatim ('BCI', 'BancoEstado', ...) or 'Manual' fallback (MID-02 / CA-02).",
                  "type": "string"
                }
              },
              "required": [
                "id",
                "fecha",
                "descripcion",
                "origen",
                "monto"
              ],
              "type": "object"
            },
            "type": "array"
          }
        },
        "required": [
          "total",
          "conteo",
          "transacciones"
        ],
        "type": "object"
      },
      "MovimientosMesResponse": {
        "additionalProperties": false,
        "description": "GET /api/movimientos — monthly transaction list (US-014).",
        "properties": {
          "periodo": {
            "description": "Resolved period, format YYYY-MM.",
            "type": "string"
          },
          "totalTransacciones": {
            "maximum": 9007199254740991,
            "minimum": -9007199254740991,
            "type": "integer"
          },
          "transacciones": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "abono": {
                  "description": "BigInt-safe decimal string amount (never a JSON number).",
                  "type": "string"
                },
                "banco": {
                  "type": "string"
                },
                "bucket": {
                  "description": "Bucket name (Bucket domain enum value).",
                  "type": "string"
                },
                "cargo": {
                  "description": "BigInt-safe decimal string amount (never a JSON number).",
                  "type": "string"
                },
                "categoria": {
                  "anyOf": [
                    {
                      "additionalProperties": false,
                      "properties": {
                        "id": {
                          "type": "string"
                        },
                        "nombre": {
                          "type": "string"
                        }
                      },
                      "required": [
                        "id",
                        "nombre"
                      ],
                      "type": "object"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "description": "Folded category, or null for Ingreso/SinCategoria rows."
                },
                "descripcion": {
                  "type": "string"
                },
                "fecha": {
                  "description": "ISO-8601 UTC timestamp.",
                  "type": "string"
                },
                "id": {
                  "type": "string"
                },
                "numeroCuenta": {
                  "type": "string"
                },
                "tipoCuenta": {
                  "type": "string"
                }
              },
              "required": [
                "id",
                "fecha",
                "descripcion",
                "cargo",
                "abono",
                "banco",
                "tipoCuenta",
                "numeroCuenta",
                "bucket",
                "categoria"
              ],
              "type": "object"
            },
            "type": "array"
          }
        },
        "required": [
          "periodo",
          "totalTransacciones",
          "transacciones"
        ],
        "type": "object"
      },
      "PatronResponse": {
        "additionalProperties": false,
        "description": "A classification pattern (US-038).",
        "properties": {
          "categoriaId": {
            "type": "string"
          },
          "id": {
            "type": "string"
          },
          "matchType": {
            "type": "string"
          },
          "patron": {
            "type": "string"
          },
          "prioridad": {
            "type": "number"
          }
        },
        "required": [
          "id",
          "categoriaId",
          "patron",
          "matchType",
          "prioridad"
        ],
        "type": "object"
      },
      "PerfilErrorResponse": {
        "additionalProperties": false,
        "description": "Error body for the /api/perfil* endpoints (US-040).",
        "properties": {
          "code": {
            "type": "string"
          },
          "message": {
            "type": "string"
          }
        },
        "required": [
          "message",
          "code"
        ],
        "type": "object"
      },
      "PreviewIngestaResponse": {
        "additionalProperties": false,
        "description": "POST /api/ingestas/preview — dry-run preview. CANONICAL: resumen + filas (full set, per-row dedup and classification, US-057). DEPRECATED (removed by US-061): estructura + muestra (first 50 rows, old shape) kept for shipped clients.",
        "properties": {
          "banco": {
            "type": "string"
          },
          "estructura": {
            "additionalProperties": false,
            "description": "DEPRECATED (US-061): legacy aggregate. Use resumen.",
            "properties": {
              "totalFilasDatos": {
                "description": "DEPRECATED (US-061): mirror of resumen.totalFilas. Kept for shipped clients.",
                "maximum": 9007199254740991,
                "minimum": -9007199254740991,
                "type": "integer"
              }
            },
            "required": [
              "totalFilasDatos"
            ],
            "type": "object"
          },
          "filas": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "abono": {
                  "description": "BigInt-safe decimal string amount (never a JSON number).",
                  "type": "string"
                },
                "cargo": {
                  "description": "BigInt-safe decimal string amount (never a JSON number).",
                  "type": "string"
                },
                "descripcion": {
                  "type": "string"
                },
                "esDuplicado": {
                  "type": "boolean"
                },
                "fecha": {
                  "description": "ISO-8601 UTC timestamp.",
                  "type": "string"
                },
                "rowIndex": {
                  "maximum": 9007199254740991,
                  "minimum": 0,
                  "type": "integer"
                },
                "sugerido": {
                  "anyOf": [
                    {
                      "additionalProperties": false,
                      "properties": {
                        "bucket": {
                          "type": "string"
                        },
                        "categoriaId": {
                          "anyOf": [
                            {
                              "type": "string"
                            },
                            {
                              "type": "null"
                            }
                          ]
                        }
                      },
                      "required": [
                        "bucket",
                        "categoriaId"
                      ],
                      "type": "object"
                    },
                    {
                      "type": "null"
                    }
                  ]
                }
              },
              "required": [
                "rowIndex",
                "fecha",
                "descripcion",
                "cargo",
                "abono",
                "esDuplicado",
                "sugerido"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "muestra": {
            "description": "DEPRECATED (US-061): legacy first-50-rows sample in the old 4-field shape. Use filas.",
            "items": {
              "additionalProperties": false,
              "properties": {
                "abono": {
                  "description": "BigInt-safe decimal string amount (never a JSON number).",
                  "type": "string"
                },
                "cargo": {
                  "description": "BigInt-safe decimal string amount (never a JSON number).",
                  "type": "string"
                },
                "descripcion": {
                  "type": "string"
                },
                "fecha": {
                  "description": "ISO-8601 UTC timestamp.",
                  "type": "string"
                }
              },
              "required": [
                "fecha",
                "descripcion",
                "cargo",
                "abono"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "numeroCuenta": {
            "type": "string"
          },
          "resumen": {
            "additionalProperties": false,
            "properties": {
              "duplicadosDetectados": {
                "maximum": 9007199254740991,
                "minimum": -9007199254740991,
                "type": "integer"
              },
              "nuevas": {
                "maximum": 9007199254740991,
                "minimum": -9007199254740991,
                "type": "integer"
              },
              "totalFilas": {
                "description": "Row count PRE-dedupe, not money — plain JSON number.",
                "maximum": 9007199254740991,
                "minimum": -9007199254740991,
                "type": "integer"
              }
            },
            "required": [
              "totalFilas",
              "duplicadosDetectados",
              "nuevas"
            ],
            "type": "object"
          },
          "tipoCuenta": {
            "type": "string"
          }
        },
        "required": [
          "banco",
          "tipoCuenta",
          "numeroCuenta",
          "estructura",
          "muestra"
        ],
        "type": "object"
      },
      "ReevaluarCategoriasResponse": {
        "additionalProperties": false,
        "description": "POST /api/transacciones/reevaluar 200 — reevaluation outcome counts.",
        "properties": {
          "transaccionesActualizadas": {
            "description": "Rows whose categoria/bucket actually changed and were written. Rows that resolved to SinCategoria (no pattern matched) are left untouched and never counted here.",
            "type": "number"
          },
          "transaccionesEvaluadas": {
            "description": "Total transactions belonging to the caller that were evaluated.",
            "type": "number"
          }
        },
        "required": [
          "transaccionesEvaluadas",
          "transaccionesActualizadas"
        ],
        "type": "object"
      },
      "RegistrarMovimientoManualResponse": {
        "additionalProperties": false,
        "description": "POST /api/movimientos 201 — the persisted manual movement (US-058, D-12). Amounts are BigInt-safe strings. `origen` is always \"Manual\".",
        "properties": {
          "abono": {
            "description": "BigInt-safe decimal string; \"0\" for Gasto rows.",
            "type": "string"
          },
          "bucket": {
            "description": "Bucket enum value (Ingreso | Necesidades | Deseos | Ahorro). Ingreso rows always carry \"Ingreso\"; Gasto rows carry the caller-supplied bucket.",
            "type": "string"
          },
          "cargo": {
            "description": "BigInt-safe decimal string; \"0\" for Ingreso rows.",
            "type": "string"
          },
          "categoriaId": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "null for Ingreso rows; the caller-supplied categoriaId for Gasto rows."
          },
          "descripcion": {
            "type": "string"
          },
          "fecha": {
            "description": "ISO-8601 UTC timestamp.",
            "type": "string"
          },
          "id": {
            "type": "string"
          },
          "origen": {
            "const": "Manual",
            "description": "Provenance marker — always \"Manual\" for this endpoint (C-a, D-13).",
            "type": "string"
          }
        },
        "required": [
          "id",
          "fecha",
          "descripcion",
          "cargo",
          "abono",
          "bucket",
          "categoriaId",
          "origen"
        ],
        "type": "object"
      },
      "ResumenAnualResponse": {
        "additionalProperties": false,
        "description": "GET /api/resumen/anual — 50/30/20 annual breakdown (US-030).",
        "properties": {
          "anio": {
            "maximum": 9007199254740991,
            "minimum": -9007199254740991,
            "type": "integer"
          },
          "meses": {
            "description": "Always 12 entries, Enero→Diciembre.",
            "items": {
              "$ref": "#/components/schemas/ResumenMesResponse"
            },
            "type": "array"
          }
        },
        "required": [
          "anio",
          "meses"
        ],
        "type": "object"
      },
      "ResumenMesResponse": {
        "additionalProperties": false,
        "description": "GET /api/resumen — 50/30/20 monthly breakdown (US-015/016).",
        "properties": {
          "buckets": {
            "description": "Always 4 entries: Necesidades, Deseos, Ahorro, SinCategoria.",
            "items": {
              "additionalProperties": false,
              "properties": {
                "bucket": {
                  "description": "Bucket name (Bucket domain enum value).",
                  "type": "string"
                },
                "estadoSemaforo": {
                  "anyOf": [
                    {
                      "enum": [
                        "verde",
                        "amarillo",
                        "rojo"
                      ],
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "description": "Traffic-light health state, lowercase wire enum (US-016), or null."
                },
                "porcentajeBp": {
                  "anyOf": [
                    {
                      "maximum": 9007199254740991,
                      "minimum": -9007199254740991,
                      "type": "integer"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "description": "Basis-point percentage (0-10000). null when sinIngreso=true."
                },
                "total": {
                  "description": "BigInt-safe decimal string amount (never a JSON number).",
                  "type": "string"
                }
              },
              "required": [
                "bucket",
                "total",
                "porcentajeBp",
                "estadoSemaforo"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "cantidadSinCategoria": {
            "description": "US-045: count of uncategorized cargo transactions (row count, not money). Always present.",
            "maximum": 9007199254740991,
            "minimum": 0,
            "type": "integer"
          },
          "estadoGlobal": {
            "anyOf": [
              {
                "enum": [
                  "verde",
                  "amarillo",
                  "rojo"
                ],
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Worst traffic-light state across measured buckets, or null."
          },
          "periodo": {
            "description": "Resolved period, format YYYY-MM.",
            "type": "string"
          },
          "sinIngreso": {
            "type": "boolean"
          },
          "targets": {
            "additionalProperties": false,
            "description": "Hardcoded 50/30/20 reference targets.",
            "properties": {
              "Ahorro": {
                "type": "number"
              },
              "Deseos": {
                "type": "number"
              },
              "Necesidades": {
                "type": "number"
              }
            },
            "required": [
              "Necesidades",
              "Deseos",
              "Ahorro"
            ],
            "type": "object"
          },
          "totalIngreso": {
            "description": "BigInt-safe decimal string amount (never a JSON number).",
            "type": "string"
          }
        },
        "required": [
          "periodo",
          "totalIngreso",
          "sinIngreso",
          "buckets",
          "targets",
          "estadoGlobal",
          "cantidadSinCategoria"
        ],
        "type": "object"
      },
      "SemaforoDetalleResponse": {
        "additionalProperties": false,
        "description": "GET /api/resumen/semaforo — semáforo detail: zone-band edges, diagnosis, and CLP-to-Verde advice (US-049).",
        "properties": {
          "buckets": {
            "description": "Always exactly 3 entries: Necesidades, Deseos, Ahorro (D-03).",
            "items": {
              "additionalProperties": false,
              "properties": {
                "bandas": {
                  "additionalProperties": false,
                  "description": "Zone-band edges used to compute this estado, sourced from the single BANDAS_SEMAFORO table (SEM-02).",
                  "properties": {
                    "amarilloMax": {
                      "maximum": 9007199254740991,
                      "minimum": -9007199254740991,
                      "type": "integer"
                    },
                    "amarilloMin": {
                      "anyOf": [
                        {
                          "maximum": 9007199254740991,
                          "minimum": -9007199254740991,
                          "type": "integer"
                        },
                        {
                          "type": "null"
                        }
                      ]
                    },
                    "verdeMax": {
                      "maximum": 9007199254740991,
                      "minimum": -9007199254740991,
                      "type": "integer"
                    },
                    "verdeMin": {
                      "anyOf": [
                        {
                          "maximum": 9007199254740991,
                          "minimum": -9007199254740991,
                          "type": "integer"
                        },
                        {
                          "type": "null"
                        }
                      ]
                    }
                  },
                  "required": [
                    "verdeMin",
                    "verdeMax",
                    "amarilloMin",
                    "amarilloMax"
                  ],
                  "type": "object"
                },
                "bucket": {
                  "description": "Bucket name (Bucket domain enum value).",
                  "type": "string"
                },
                "consejo": {
                  "anyOf": [
                    {
                      "additionalProperties": false,
                      "properties": {
                        "direccion": {
                          "enum": [
                            "reducir",
                            "aumentar"
                          ],
                          "type": "string"
                        },
                        "mensaje": {
                          "description": "Spanish sentence containing the literal `{monto}` placeholder exactly once (SEM-10).",
                          "type": "string"
                        },
                        "monto": {
                          "description": "BigInt-safe decimal string amount (never a JSON number).",
                          "type": "string"
                        }
                      },
                      "required": [
                        "direccion",
                        "monto",
                        "mensaje"
                      ],
                      "type": "object"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "description": "CLP-to-Verde advice (SEM-03/SEM-04). null when estadoSemaforo is verde or null."
                },
                "estadoSemaforo": {
                  "anyOf": [
                    {
                      "enum": [
                        "verde",
                        "amarillo",
                        "rojo"
                      ],
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "description": "Traffic-light health state, lowercase wire enum, or null."
                },
                "metaBp": {
                  "description": "Bucket's 50/30/20 target center, in basis points (SEM-02).",
                  "maximum": 9007199254740991,
                  "minimum": -9007199254740991,
                  "type": "integer"
                },
                "porcentajeBp": {
                  "anyOf": [
                    {
                      "maximum": 9007199254740991,
                      "minimum": -9007199254740991,
                      "type": "integer"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "description": "Basis-point percentage (0-10000). null when sinIngreso=true."
                },
                "total": {
                  "description": "BigInt-safe decimal string amount (never a JSON number).",
                  "type": "string"
                }
              },
              "required": [
                "bucket",
                "total",
                "porcentajeBp",
                "estadoSemaforo",
                "metaBp",
                "bandas",
                "consejo"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "bucketsCriticos": {
            "description": "Bucket names sharing estadoGlobal severity. [] when Verde or sinIngreso.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "diagnostico": {
            "description": "Backend-generated Spanish sentence naming the driving bucket(s) (SEM-01). Never contains {monto}.",
            "type": "string"
          },
          "estadoGlobal": {
            "anyOf": [
              {
                "enum": [
                  "verde",
                  "amarillo",
                  "rojo"
                ],
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Worst traffic-light state across the 3 spend buckets, or null."
          },
          "periodo": {
            "description": "Resolved period, format YYYY-MM.",
            "type": "string"
          },
          "sinCategoria": {
            "additionalProperties": false,
            "description": "Re-exposed from /api/resumen, never recomputed independently (SEM-05).",
            "properties": {
              "cantidad": {
                "maximum": 9007199254740991,
                "minimum": 0,
                "type": "integer"
              },
              "total": {
                "description": "BigInt-safe decimal string amount (never a JSON number).",
                "type": "string"
              }
            },
            "required": [
              "cantidad",
              "total"
            ],
            "type": "object"
          },
          "sinIngreso": {
            "type": "boolean"
          },
          "totalIngreso": {
            "description": "BigInt-safe decimal string amount (never a JSON number).",
            "type": "string"
          }
        },
        "required": [
          "periodo",
          "totalIngreso",
          "sinIngreso",
          "estadoGlobal",
          "diagnostico",
          "bucketsCriticos",
          "buckets",
          "sinCategoria"
        ],
        "type": "object"
      },
      "TransaccionesCategoriaResponse": {
        "additionalProperties": false,
        "description": "PATCH /api/transacciones/:id/categoria — manual reclassification result (US-013 S4).",
        "properties": {
          "bucket": {
            "description": "Derived bucket name (echo, not raw input).",
            "type": "string"
          },
          "categoria": {
            "additionalProperties": false,
            "properties": {
              "id": {
                "type": "string"
              },
              "nombre": {
                "type": "string"
              }
            },
            "required": [
              "id",
              "nombre"
            ],
            "type": "object"
          },
          "id": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "categoria",
          "bucket"
        ],
        "type": "object"
      },
      "VersionResponse": {
        "additionalProperties": false,
        "description": "Deployed build info for the API — GET /version.",
        "properties": {
          "builtAt": {
            "description": "ISO-8601 timestamp of when this process booted.",
            "type": "string"
          },
          "commit": {
            "description": "Short commit SHA, or \"local\" outside Render.",
            "type": "string"
          },
          "ref": {
            "description": "Git branch/ref, or \"local\" outside Render.",
            "type": "string"
          },
          "version": {
            "description": "Package version (release-please, ADR-030).",
            "type": "string"
          }
        },
        "required": [
          "version",
          "commit",
          "ref",
          "builtAt"
        ],
        "type": "object"
      },
      "VincularGoogleResponse": {
        "additionalProperties": false,
        "description": "POST /api/perfil/google/vincular — the Google OAuth authorization URL to navigate to (VINC041-01).",
        "properties": {
          "urlAutorizacion": {
            "type": "string"
          }
        },
        "required": [
          "urlAutorizacion"
        ],
        "type": "object"
      }
    }
  },
  "info": {
    "description": "HTTP contract for the MoneyDiary Express API, sourced from Zod schemas (ADR-011 amend).",
    "title": "MoneyDiary API",
    "version": "0.5.1"
  },
  "openapi": "3.1.0",
  "paths": {
    "/api/auth/capabilities": {
      "get": {
        "description": "Public endpoint (requires x-api-key only, session-public — no prior session needed), always mounted regardless of whether GOOGLE_CLIENT_ID/GOOGLE_CLIENT_SECRET (web) or GOOGLE_CLIENT_ID_ANDROID (mobile) are configured (AC-10). Each client reads its own field (googleLoginEnabled or googleLoginMobileEnabled) before rendering its own Google-login affordance.",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AuthCapabilitiesResponse"
                }
              }
            },
            "description": "Current activation state of Google login."
          },
          "401": {
            "description": "Missing or invalid x-api-key."
          }
        },
        "summary": "Discover whether web and/or mobile Google login are active"
      }
    },
    "/api/auth/demo": {
      "get": {
        "description": "Public endpoint (requires x-api-key only) that creates a demo account or resumes an existing valid session, then redirects. No JSON response body — the session is conveyed via the `md_session` cookie (Set-Cookie) and the redirect target.",
        "responses": {
          "302": {
            "description": "Session established (new demo or resumed existing); redirects to the app root.",
            "headers": {
              "Location": {
                "description": "Always \"/\" — the app root.",
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "403": {
            "description": "Rejected: request is not a top-level navigation (anti-embed Fetch-Metadata guard)."
          },
          "429": {
            "description": "Rate-limited: too many demo requests from this IP."
          }
        },
        "summary": "Create or resume a demo session"
      }
    },
    "/api/auth/google": {
      "get": {
        "description": "Public endpoint (requires x-api-key only, session-public) that starts the OIDC Authorization Code + PKCE round trip with Google (AUTH-11). Must be reached via a true top-level browser navigation — see AUTH-17. Sets the short-lived `md_oauth` cookie (state/nonce/PKCE) and redirects to Google. 404 when Google login is not active (AUTH-16) — see GET /api/auth/capabilities.",
        "responses": {
          "302": {
            "description": "Redirects to Google's OAuth 2.0 authorization endpoint.",
            "headers": {
              "Location": {
                "description": "Google's authorization URL.",
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "403": {
            "description": "Rejected: request is not a top-level navigation (anti-embed Fetch-Metadata guard, shared with GET /api/auth/demo)."
          },
          "404": {
            "description": "Google login is not active — GOOGLE_CLIENT_ID/GOOGLE_CLIENT_SECRET are not both configured (AUTH-16)."
          },
          "429": {
            "description": "Rate-limited: too many Google login attempts from this IP (budget shared with the callback, design §6.4)."
          }
        },
        "summary": "Start Google sign-in"
      }
    },
    "/api/auth/google/callback": {
      "get": {
        "description": "Public endpoint (requires x-api-key only, session-public) — Google's redirect target after consent. Validates `state` against the `md_oauth` cookie and the `id_token` (signature/iss/aud/exp/nonce) before any identity resolution (AUTH-12), then resolves the identity — matching an existing user by googleSub or email, or CREATING a new passwordless account on first verified login (signup-on-first-login, ADR-041) — and issues a session equivalent to password login (AUTH-13). Every failure cause — bad state, bad token, unverified email, a lost account-creation race, an unexpected infra fault — produces the identical 302 redirect (AUTH-15): this contract intentionally does not distinguish them, including whether the session belongs to a pre-existing or a just-created account. DUAL MODE (US-041, VINC041-02/03): when `md_oauth` carries a signed `link` marker (set by POST /api/perfil/google/vincular), this same endpoint completes an EXPLICIT LINK instead of a login/signup — it binds a Google identity to the CALLER's own account, issues NO new session, never creates an account, and redirects to `/configuracion?google=vinculado` on success or `/configuracion?google=error` on a modelled failure. A `link` marker that fails its integrity check rejects the WHOLE callback to the generic `/login?error=google` — it never falls back to the login/signup path.",
        "responses": {
          "302": {
            "description": "Success: sets `md_session` and redirects to \"/\". Failure (any cause): redirects to \"/login?error=google\" with no session — see AUTH-15.",
            "headers": {
              "Location": {
                "description": "\"/\" on success, \"/login?error=google\" on any failure.",
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "403": {
            "description": "Rejected: request is not a top-level navigation (same Fetch-Metadata guard as initiate, design §3)."
          },
          "404": {
            "description": "Google login is not active — GOOGLE_CLIENT_ID/GOOGLE_CLIENT_SECRET are not both configured (AUTH-16)."
          },
          "429": {
            "description": "Rate-limited: too many Google login attempts from this IP (budget shared with initiate, design §6.4)."
          }
        },
        "summary": "Complete Google sign-in"
      }
    },
    "/api/auth/google/token": {
      "post": {
        "description": "Public endpoint (requires x-api-key only, session-public — no prior session needed) that verifies a device-obtained Google id_token (AUTH-19) and resolves the identity — matching an existing user by googleSub or email, or CREATING a new passwordless account on first verified login (signup-on-first-login, ADR-041, same use case as the web callback) — issuing a session identical in shape to POST /api/auth/login (AUTH-20) for BOTH outcomes; the response never reveals whether the account is pre-existing or just-created. Every failure cause — invalid/expired/wrong-audience token, unverified email, a demo-user match, an email already linked to a different googleSub, a lost account-creation race, or a JWKS/network failure — produces the identical 401 body used by POST /api/auth/login (AUTH-21, anti-enumeration). No Set-Cookie: mobile uses Bearer + SecureStore. A successful login of a PRE-EXISTING user releases this endpoint's own IP rate-limit budget; a successful SIGNUP never does (mass-signup defense, ADR-041 Consecuencias) — the budget still caps the rate of new accounts from a single IP. 404 when GOOGLE_CLIENT_ID_ANDROID is not configured (AUTH-22) — independent of GET /api/auth/google's activation gate.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "idToken": {
                    "type": "string"
                  }
                },
                "required": [
                  "idToken"
                ],
                "type": "object"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AuthLoginResponse"
                }
              }
            },
            "description": "Authentication succeeded — identical shape to POST /api/auth/login."
          },
          "401": {
            "description": "Verification or identity resolution failed (scrubbed — never echoes the id_token or email; identical body to POST /api/auth/login for every cause, AUTH-21)."
          },
          "404": {
            "description": "Google login mobile is not active — GOOGLE_CLIENT_ID_ANDROID is not configured (AUTH-22)."
          },
          "429": {
            "description": "Rate-limited: too many attempts from this IP (own budget, distinct from /auth/login and GET /api/auth/google, design §6.4)."
          }
        },
        "summary": "Authenticate with a native Google id_token (mobile)"
      }
    },
    "/api/auth/login": {
      "post": {
        "description": "Public endpoint (requires x-api-key only, session-public — no prior session needed) that authenticates a user and returns a session token (AUTH-01). Sets the `md_session` cookie (Set-Cookie) in addition to the JSON body.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "email": {
                    "type": "string"
                  },
                  "password": {
                    "type": "string"
                  }
                },
                "required": [
                  "email",
                  "password"
                ],
                "type": "object"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AuthLoginResponse"
                }
              }
            },
            "description": "Authentication succeeded."
          },
          "401": {
            "description": "Invalid credentials (scrubbed — never echoes email/password)."
          },
          "429": {
            "description": "Rate-limited: too many failed login attempts from this IP/email combination."
          }
        },
        "summary": "Authenticate with email and password"
      }
    },
    "/api/auth/logout": {
      "post": {
        "description": "Public endpoint (requires x-api-key only, session-public) that invalidates the current session token (if any) and clears the `md_session` cookie. Always succeeds — an already missing or invalid token does not produce an error.",
        "responses": {
          "204": {
            "description": "Session ended (or was already absent). No response body."
          }
        },
        "summary": "End the current session"
      }
    },
    "/api/auth/me": {
      "get": {
        "description": "Authenticated endpoint returning the identity of the current session (AUTH-09, DEMO-AUTH-05). Requires x-api-key + a valid session.",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AuthMeResponse"
                }
              }
            },
            "description": "Identity of the authenticated session."
          },
          "401": {
            "description": "No valid session (missing, expired, or invalid token)."
          }
        },
        "summary": "Current session identity"
      }
    },
    "/api/buckets/{bucket}": {
      "get": {
        "description": "Authenticated endpoint returning the transaction detail for a single spend bucket (US-017). Requires x-api-key + a valid session (RNF-SEC-006, per-user isolation).",
        "parameters": [
          {
            "description": "Bucket name (raw path param). Validated against the domain enum by the use case, not this schema.",
            "in": "path",
            "name": "bucket",
            "required": true,
            "schema": {
              "description": "Bucket name (raw path param). Validated against the domain enum by the use case, not this schema.",
              "type": "string"
            }
          },
          {
            "description": "Month period, format YYYY-MM (e.g. 2026-07). Absent defaults to the current month. Format is validated by the domain, not this schema.",
            "in": "query",
            "name": "periodo",
            "schema": {
              "description": "Month period, format YYYY-MM (e.g. 2026-07). Absent defaults to the current month. Format is validated by the domain, not this schema.",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DetalleBucketResponse"
                }
              }
            },
            "description": "Bucket detail for the resolved period."
          },
          "400": {
            "description": "Invalid bucket (unrecognized value) or invalid periodo — both domain-level (BucketInvalidoError / PeriodoInvalidoError)."
          }
        },
        "summary": "Bucket drill-down"
      }
    },
    "/api/buckets/{bucket}/detalle": {
      "get": {
        "description": "Authenticated sibling detail endpoint to GET /api/buckets/{bucket} (US-051): returns the month×bucket detail GROUPED by category — a header with totals and % vs meta, and category groups carrying ALL their transactions (BigInt-safe strings, no account PII per MBD-08). Accepts only the four spend buckets (Necesidades, Deseos, Ahorro, SinCategoria); Ingresos is out of scope (US-052) and rejected with a scrubbed 400. Requires x-api-key + a valid session (RNF-SEC-006, per-user isolation, ISO-01/ISO-02).",
        "parameters": [
          {
            "description": "Bucket name (raw path param). Validated against the domain enum by the use case, not this schema.",
            "in": "path",
            "name": "bucket",
            "required": true,
            "schema": {
              "description": "Bucket name (raw path param). Validated against the domain enum by the use case, not this schema.",
              "type": "string"
            }
          },
          {
            "description": "Month period, format YYYY-MM (e.g. 2026-07). Absent defaults to the current month. Format is validated by the domain, not this schema.",
            "in": "query",
            "name": "periodo",
            "schema": {
              "description": "Month period, format YYYY-MM (e.g. 2026-07). Absent defaults to the current month. Format is validated by the domain, not this schema.",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BucketDetalleMesResponse"
                }
              }
            },
            "description": "Month×bucket detail grouped by category for the resolved period (MBD-01/02/03/05/08)."
          },
          "400": {
            "description": "Invalid bucket (outside the 4-bucket allowlist, e.g. Ingresos) or invalid periodo — both domain-level (BucketInvalidoError / PeriodoInvalidoError), scrubbed messages."
          }
        },
        "summary": "Bucket detail grouped by category for a month"
      }
    },
    "/api/categorias": {
      "get": {
        "description": "Authenticated endpoint returning the caller's own categories with their nested classification patterns and an all-history `transaccionesCount` per category — the caller-scoped impact preview for a destructive delete (US-038, CAT038-02; US-039, CAT039-01). Requires x-api-key + a valid session (RNF-SEC-006, per-user isolation). Available to demo sessions (read-only, CAT038-08).",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogoResponse"
                }
              }
            },
            "description": "The caller's full catalog."
          }
        },
        "summary": "List the caller's category catalog"
      },
      "post": {
        "description": "Authenticated endpoint that creates a category owned by the caller (US-038, CAT038-01), optionally with its classification patrones created atomically in the same call (CAT038-10/11) — when `patrones` is omitted or empty, the contract is byte-identical to the pre-existing behavior. Requires x-api-key + a valid session. Rejected for demo sessions (403 DEMO_SOLO_LECTURA).",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "additionalProperties": false,
                "properties": {
                  "bucket": {
                    "type": "string"
                  },
                  "nombre": {
                    "type": "string"
                  },
                  "patrones": {
                    "items": {
                      "additionalProperties": false,
                      "properties": {
                        "matchType": {
                          "type": "string"
                        },
                        "patron": {
                          "type": "string"
                        }
                      },
                      "required": [
                        "patron",
                        "matchType"
                      ],
                      "type": "object"
                    },
                    "maxItems": 20,
                    "type": "array"
                  }
                },
                "required": [
                  "nombre",
                  "bucket"
                ],
                "type": "object"
              }
            }
          }
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CategoriaResponse"
                }
              }
            },
            "description": "Category created, with its created patrones (if any) nested."
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogoErrorResponse"
                }
              }
            },
            "description": "Invalid nombre/bucket, a malformed request body, or a nested patrón that failed validation (CAT038-11) — in the latter case the body carries `indice`, the zero-based position of the offending entry within the submitted `patrones` array."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogoErrorResponse"
                }
              }
            },
            "description": "The calling session is a demo session (read-only catalog)."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogoErrorResponse"
                }
              }
            },
            "description": "A category with that nombre already exists for this user (case-insensitive), or a nested patrón collides with one already owned by the caller or with another entry in the same submitted `patrones` array (CAT038-11) — the latter case carries `indice`."
          }
        },
        "summary": "Create a category"
      }
    },
    "/api/categorias/{id}": {
      "delete": {
        "description": "Authenticated endpoint that deletes a category and cascades its patterns, atomically (US-038, CAT038-04 as modified by US-039). The delete always succeeds for a category owned by the caller, whether it is referenced by transactions or not. Every Transaccion that referenced the deleted category survives with categoriaId: null and its original bucketId unchanged — no money moves between buckets. Requires x-api-key + a valid session. Rejected for demo sessions.",
        "parameters": [
          {
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Category (and its patterns) deleted. No response body."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogoErrorResponse"
                }
              }
            },
            "description": "The calling session is a demo session (read-only catalog)."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogoErrorResponse"
                }
              }
            },
            "description": "Anti-enumeration: the category does not exist or does not belong to the authenticated user."
          }
        },
        "summary": "Delete a category"
      },
      "patch": {
        "description": "Authenticated endpoint that partially updates a category (US-038, CAT038-03). At least one of `nombre`/`bucket` MUST be present. When `bucket` actually changes, every historical Transaccion pointing at the category is re-stamped atomically. Requires x-api-key + a valid session. Rejected for demo sessions.",
        "parameters": [
          {
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "additionalProperties": false,
                "properties": {
                  "bucket": {
                    "type": "string"
                  },
                  "nombre": {
                    "type": "string"
                  }
                },
                "type": "object"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CategoriaResponse"
                }
              }
            },
            "description": "Category updated."
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogoErrorResponse"
                }
              }
            },
            "description": "Invalid nombre/bucket, an empty body, or a malformed request body."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogoErrorResponse"
                }
              }
            },
            "description": "The calling session is a demo session (read-only catalog)."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogoErrorResponse"
                }
              }
            },
            "description": "Anti-enumeration: the category does not exist or does not belong to the authenticated user."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogoErrorResponse"
                }
              }
            },
            "description": "A category with that nombre already exists for this user (case-insensitive)."
          }
        },
        "summary": "Rename and/or re-bucket a category"
      }
    },
    "/api/ingestas": {
      "get": {
        "description": "Authenticated endpoint returning the per-user ingesta history (US-004/US-018). Requires x-api-key + a valid session (RNF-SEC-006, per-user isolation).",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/IngestasListResponse"
                }
              }
            },
            "description": "Ingesta list for the authenticated user."
          }
        },
        "summary": "List ingestas"
      },
      "post": {
        "deprecated": true,
        "description": "Authenticated endpoint that detects the bank, validates structure, normalizes, persists, and categorizes a bank statement file (US-004/US-005/US-011). Requires x-api-key + a valid session (RNF-SEC-006, per-user isolation). Rejected for demo sessions (403 DEMO_SOLO_LECTURA). DEPRECATED at US-057 (D-14/CA-05): this one-shot endpoint is superseded by the two-step POST /api/ingestas/preview → POST /api/ingestas/commit flow. Physical removal is tracked by US-061. Behavior is UNCHANGED — existing callers (mobile, ADR-026) continue to work.",
        "requestBody": {
          "content": {
            "multipart/form-data": {
              "schema": {
                "properties": {
                  "file": {
                    "contentEncoding": "binary",
                    "description": "Bank statement file (.xlsx or .pdf). Extension/bank-format validation is a domain rule (ExtensionNoPermitidaError / BancoNoReconocidoError), not this schema.",
                    "format": "binary",
                    "type": "string"
                  }
                },
                "required": [
                  "file"
                ],
                "type": "object"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/IngestaUploadResponse"
                }
              }
            },
            "description": "Upload processed and persisted."
          },
          "400": {
            "description": "Invalid file — missing file field, disallowed extension, unrecognized bank, invalid structure/normalization, or an oversized file (>10 MB)."
          },
          "403": {
            "description": "The calling session is a demo session (issue #500)."
          },
          "500": {
            "description": "Persistence failure (infrastructure fault, not the uploaded file)."
          }
        },
        "summary": "Upload a bank statement (deprecated — use POST /api/ingestas/commit)"
      }
    },
    "/api/ingestas/{id}": {
      "delete": {
        "description": "Authenticated endpoint that cascade-deletes an ingesta and its transactions (US-018, ING-01/ING-02). Requires x-api-key + a valid session (RNF-SEC-006, per-user isolation). Rejected for demo sessions (403 DEMO_SOLO_LECTURA).",
        "parameters": [
          {
            "description": "Ingesta id (raw path param). Existence/ownership is checked by the use case, not this schema.",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "Ingesta id (raw path param). Existence/ownership is checked by the use case, not this schema.",
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Ingesta deleted. No response body."
          },
          "403": {
            "description": "The calling session is a demo session (issue #500). Nothing is deleted."
          },
          "404": {
            "description": "Anti-enumeration: the ingesta does not exist or does not belong to the authenticated user."
          }
        },
        "summary": "Delete an ingesta"
      }
    },
    "/api/ingestas/commit": {
      "post": {
        "description": "Authenticated endpoint — the second step of the preview → commit flow. Re-parses the same file, deduplicates against the calling user's history, applies the optional classification overlay (`edits` JSON text field, ≤256 KB), auto-classifies remaining rows, and persists atomically. New duplicates found at commit time are omitted and counted in `duplicadosOmitidos` (commit never aborts on duplicates, CA-03). Absent/empty `edits` ⇒ pure auto-classify (equivalent to the deprecated one-shot for the transacciones payload). Requires x-api-key + a valid session (RNF-SEC-006, per-user isolation). Rejected for demo sessions (403 DEMO_SOLO_LECTURA). Overlay errors (malformed edits, out-of-range rowIndex, cross-tenant categoriaId) return 400 and persist nothing (D-03/D-04/D-10).",
        "requestBody": {
          "content": {
            "multipart/form-data": {
              "schema": {
                "properties": {
                  "edits": {
                    "description": "JSON text field carrying the edit overlay: Array<{ rowIndex: number, categoriaId: string | null }>. Absent or empty ⇒ empty overlay (pure auto-classify commit). Max 256 KB, enforced by multer limits.fieldSize (D-02).",
                    "type": "string"
                  },
                  "file": {
                    "contentEncoding": "binary",
                    "description": "Bank statement file (.xlsx or .pdf). Extension/bank-format validation is a domain rule (ExtensionNoPermitidaError / BancoNoReconocidoError), not this schema.",
                    "format": "binary",
                    "type": "string"
                  }
                },
                "required": [
                  "file"
                ],
                "type": "object"
              }
            }
          }
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CommitIngestaResponse"
                }
              }
            },
            "description": "Import committed and persisted. Response carries per-row bucket + categoriaId."
          },
          "400": {
            "description": "Invalid file (extension, bank, structure, normalization) OR malformed/invalid edits (EdicionesInvalidasError, RowIndexFueraDeRangoError, CategoriaFueraDeCatalogoError). Nothing is persisted. Amounts are scrubbed from every error message (ADR-013)."
          },
          "403": {
            "description": "The calling session is a demo session (issue #500). Nothing is persisted."
          },
          "500": {
            "description": "Infrastructure fault (DB) — ensure, dedup, catalog load, or persist failure (PersistenciaFallidaError / CategorizacionFallidaError). Retryable."
          }
        },
        "summary": "Commit a bank statement import with optional edits overlay (US-057)"
      }
    },
    "/api/ingestas/preview": {
      "post": {
        "description": "Authenticated endpoint that detects the bank, validates structure, deduplicates and auto-classifies a bank statement WITHOUT persisting anything (US-057). CANONICAL response: `resumen` + `filas` (ALL rows, no sample cap, with per-row dedup status `esDuplicado` and classification `sugerido`). DEPRECATED fields (removed by US-061 alongside the one-shot endpoint): `estructura` (mirror of `resumen.totalFilas`) and `muestra` (first 50 rows in the old 4-field shape) — kept for shipped clients (deployed mobile APK) and clients pending migration. Requires x-api-key + a valid session; dedup is scoped to the calling user (RNF-SEC-006).",
        "requestBody": {
          "content": {
            "multipart/form-data": {
              "schema": {
                "properties": {
                  "file": {
                    "contentEncoding": "binary",
                    "description": "Bank statement file (.xlsx or .pdf). Extension/bank-format validation is a domain rule (ExtensionNoPermitidaError / BancoNoReconocidoError), not this schema.",
                    "format": "binary",
                    "type": "string"
                  }
                },
                "required": [
                  "file"
                ],
                "type": "object"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PreviewIngestaResponse"
                }
              }
            },
            "description": "Preview sample computed (not persisted)."
          },
          "400": {
            "description": "Invalid file — missing file field, disallowed extension, unrecognized bank, invalid structure/normalization, or an oversized file (>10 MB)."
          }
        },
        "summary": "Preview a bank statement (dry run, US-057)"
      }
    },
    "/api/ingresos/mes": {
      "get": {
        "description": "Authenticated sibling endpoint to GET /api/buckets/{bucket}/detalle (US-052): returns the monthly income detail — a header with the total Σ abono (BigInt-safe string) and count, and ALL transactions with their origin = bank NAME verbatim (CA-02, MID-02) or \"Manual\", never account PII (tipoCuenta/numeroCuenta, MID-06). Top-level path, NOT a buckets sub-resource: GET /api/buckets/Ingresos/detalle keeps rejecting Ingresos with its own scrubbed 400 (MBD-07, US-051). Requires x-api-key + a valid session (RNF-SEC-006, per-user isolation, ISO-01/ISO-02).",
        "parameters": [
          {
            "description": "Month period, format YYYY-MM (e.g. 2026-07). Absent defaults to the current month. Format is validated by the domain, not this schema.",
            "in": "query",
            "name": "periodo",
            "schema": {
              "description": "Month period, format YYYY-MM (e.g. 2026-07). Absent defaults to the current month. Format is validated by the domain, not this schema.",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/IngresosMesResponse"
                }
              }
            },
            "description": "Monthly income list for the resolved period — exactly {total, conteo, transacciones} with origen = bank name verbatim or \"Manual\" (MID-01/02/03/05)."
          },
          "400": {
            "description": "Invalid periodo — malformed YYYY-MM value (domain-level, PeriodoMes VO → PeriodoInvalidoError, MID-04); the scrubbed message never echoes the raw input."
          }
        },
        "summary": "Monthly income list by origin (bank/Manual)"
      }
    },
    "/api/movimientos": {
      "get": {
        "description": "Authenticated endpoint returning the consolidated monthly transaction list (US-014). Requires x-api-key + a valid session (RNF-SEC-006, per-user isolation).",
        "parameters": [
          {
            "description": "Month period, format YYYY-MM (e.g. 2026-07). Absent defaults to the current month. Format is validated by the domain, not this schema.",
            "in": "query",
            "name": "periodo",
            "schema": {
              "description": "Month period, format YYYY-MM (e.g. 2026-07). Absent defaults to the current month. Format is validated by the domain, not this schema.",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MovimientosMesResponse"
                }
              }
            },
            "description": "Monthly transaction list for the resolved period."
          },
          "400": {
            "description": "Invalid periodo — either a transport-shape mismatch or a malformed YYYY-MM value (domain-level, PeriodoMes VO)."
          }
        },
        "summary": "Monthly transaction list"
      },
      "post": {
        "description": "Authenticated endpoint that registers a single hand-typed movement with no cartola (US-058, D-12). Type-first: `tipo=Ingreso` auto-classifies to {bucket=Ingreso, categoriaId=null}; `tipo=Gasto` requires `bucket` ∈ {Necesidades, Deseos, Ahorro} and a `categoriaId` from the caller's own catalog. The Ingreso variant is strict (stray `bucket`/`categoriaId` ⇒ 400, Q3 resolution). `monto` is a BigInt-safe decimal string (JSON number ⇒ 400). `fecha` must be YYYY-MM-DD; domain enforces ≤ today (future ⇒ 400). A per-user sentinel Account (banco='Manual') is created lazily (MAN-04). The persisted row has `ingestaId=null`, `origen='Manual'`, and is immune to `DELETE /api/ingestas/:id` (SQL NULL-equality, CA-04). Requires x-api-key + a valid session (RNF-SEC-006, per-user isolation, ISO-01/ISO-02).",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "anyOf": [
                  {
                    "additionalProperties": false,
                    "description": "Ingreso variant — strict (stray bucket/categoriaId ⇒ 400, Q3 resolution). Auto-classified to {bucket=Ingreso, categoriaId=null} by construction (D-10).",
                    "properties": {
                      "descripcion": {
                        "type": "string"
                      },
                      "fecha": {
                        "description": "Date of the movement, YYYY-MM-DD. Domain enforces ≤ today (future ⇒ 400).",
                        "type": "string"
                      },
                      "monto": {
                        "description": "BigInt-safe decimal string (never a JSON number). Domain enforces: positive integer, no float, overflow guard (> Number.MAX_SAFE_INTEGER ⇒ 400).",
                        "type": "string"
                      },
                      "tipo": {
                        "const": "Ingreso",
                        "type": "string"
                      }
                    },
                    "required": [
                      "tipo",
                      "fecha",
                      "descripcion",
                      "monto"
                    ],
                    "type": "object"
                  },
                  {
                    "description": "Gasto variant — requires bucket ∈ {Necesidades, Deseos, Ahorro} and categoriaId.",
                    "properties": {
                      "bucket": {
                        "description": "Required for Gasto. One of Necesidades | Deseos | Ahorro. Ingreso and SinCategoria are invalid here (D-12).",
                        "enum": [
                          "Necesidades",
                          "Deseos",
                          "Ahorro"
                        ],
                        "type": "string"
                      },
                      "categoriaId": {
                        "description": "Required for Gasto. Must be a non-empty string belonging to the calling user catalog AND map to the supplied bucket (CategoriaFueraDeCatalogoError / BucketCategoriaNoConcuerdaError => 400).",
                        "minLength": 1,
                        "type": "string"
                      },
                      "descripcion": {
                        "type": "string"
                      },
                      "fecha": {
                        "description": "Date of the movement, YYYY-MM-DD.",
                        "type": "string"
                      },
                      "monto": {
                        "description": "BigInt-safe decimal string.",
                        "type": "string"
                      },
                      "tipo": {
                        "const": "Gasto",
                        "type": "string"
                      }
                    },
                    "required": [
                      "tipo",
                      "fecha",
                      "descripcion",
                      "monto",
                      "bucket",
                      "categoriaId"
                    ],
                    "type": "object"
                  }
                ]
              }
            }
          }
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegistrarMovimientoManualResponse"
                }
              }
            },
            "description": "Movement registered. Response carries id, fecha (ISO), descripcion (plaintext), cargo/abono (BigInt-safe strings), bucket, categoriaId, and origen=\"Manual\" (D-08/D-12). No DB read-back — values sourced from the in-memory VO."
          },
          "400": {
            "description": "Shape mismatch (Zod: stray fields on Ingreso, missing bucket/categoriaId on Gasto, monto as JSON number, wrong fecha format) OR domain validation failure (MovimientoManualInvalidoError: FECHA_FUTURA, DESCRIPCION_VACIA/LARGA, MONTO_INVALIDO/OVERFLOW, SIN_MONTOS, MONTO_NEGATIVO) OR catalog cascade failure (CategoriaFueraDeCatalogoError, BucketCategoriaNoConcuerdaError). Amounts are scrubbed from every error message (ADR-013)."
          },
          "500": {
            "description": "Infrastructure fault — sentinel upsert, catalog load, or Transaccion.create failed (PersistenciaFallidaError). Retryable."
          }
        },
        "summary": "Register a manual movement (type-first: Ingreso or Gasto)"
      }
    },
    "/api/movimientos/{id}": {
      "delete": {
        "description": "Authenticated endpoint that deletes a Transaccion owned by the calling user with origen='Manual' (correccion-movimientos-manuales, ADR-040). Ingesta-born rows are never individually deletable — delete the whole ingesta instead (DELETE /api/ingestas/:id). Requires x-api-key + a valid session (RNF-SEC-006, per-user isolation). Rejected for demo sessions (403 DEMO_SOLO_LECTURA).",
        "parameters": [
          {
            "description": "Transaccion id (raw path param). Existence/ownership/provenance is checked by the use case, not this schema.",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "Transaccion id (raw path param). Existence/ownership/provenance is checked by the use case, not this schema.",
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Movement deleted. No response body."
          },
          "403": {
            "description": "The calling session is a demo session. Nothing is deleted."
          },
          "404": {
            "description": "Anti-enumeration: the transaction does not exist, does not belong to the authenticated user, or is not origen='Manual' (ingesta-born rows collapse into this same response)."
          }
        },
        "summary": "Delete a manual movement"
      }
    },
    "/api/patrones": {
      "post": {
        "description": "Authenticated endpoint that creates a classification pattern under one of the caller's own categories (US-038, CAT038-05/06). Requires x-api-key + a valid session. Rejected for demo sessions.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "additionalProperties": false,
                "properties": {
                  "categoriaId": {
                    "type": "string"
                  },
                  "matchType": {
                    "type": "string"
                  },
                  "patron": {
                    "type": "string"
                  },
                  "prioridad": {
                    "type": "number"
                  }
                },
                "required": [
                  "categoriaId",
                  "patron",
                  "matchType"
                ],
                "type": "object"
              }
            }
          }
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PatronResponse"
                }
              }
            },
            "description": "Pattern created."
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogoErrorResponse"
                }
              }
            },
            "description": "Invalid patron/matchType/prioridad, an invalid REGEX (write-time compile check), or a malformed request body."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogoErrorResponse"
                }
              }
            },
            "description": "The calling session is a demo session (read-only catalog)."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogoErrorResponse"
                }
              }
            },
            "description": "Anti-enumeration: categoriaId does not exist or does not belong to the authenticated user."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogoErrorResponse"
                }
              }
            },
            "description": "A pattern with that text already exists for this user (case-insensitive)."
          }
        },
        "summary": "Create a classification pattern"
      }
    },
    "/api/patrones/{id}": {
      "delete": {
        "description": "Authenticated endpoint that deletes a pattern (US-038, CAT038-05/07). Requires x-api-key + a valid session. Rejected for demo sessions.",
        "parameters": [
          {
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Pattern deleted. No response body."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogoErrorResponse"
                }
              }
            },
            "description": "The calling session is a demo session (read-only catalog)."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogoErrorResponse"
                }
              }
            },
            "description": "Anti-enumeration: the pattern does not exist or does not belong to the authenticated user."
          }
        },
        "summary": "Delete a classification pattern"
      },
      "patch": {
        "description": "Authenticated endpoint that partially updates a pattern (US-038, CAT038-05). `categoriaId` is NOT accepted — moving a pattern between categories is a non-goal. Requires x-api-key + a valid session. Rejected for demo sessions.",
        "parameters": [
          {
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "additionalProperties": false,
                "properties": {
                  "matchType": {
                    "type": "string"
                  },
                  "patron": {
                    "type": "string"
                  },
                  "prioridad": {
                    "type": "number"
                  }
                },
                "type": "object"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PatronResponse"
                }
              }
            },
            "description": "Pattern updated."
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogoErrorResponse"
                }
              }
            },
            "description": "Invalid patron/matchType/prioridad, an invalid REGEX, an empty body, or a malformed request body."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogoErrorResponse"
                }
              }
            },
            "description": "The calling session is a demo session (read-only catalog)."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogoErrorResponse"
                }
              }
            },
            "description": "Anti-enumeration: the pattern does not exist or does not belong to the authenticated user."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogoErrorResponse"
                }
              }
            },
            "description": "A pattern with that text already exists for this user (case-insensitive)."
          }
        },
        "summary": "Update a classification pattern"
      }
    },
    "/api/perfil": {
      "patch": {
        "description": "Authenticated endpoint that updates the caller's own nombre and/or email (US-040, PERF040-01/02/03/04/07). `passwordActual` is REQUIRED whenever `email` is present. On an email change the ciphertext and blind index are rewritten together in one atomic update, so login keeps working with the NEW address and stops working with the old one. A wrong `passwordActual` and an email already claimed by another account return the SAME generic 403 PERFIL_RECHAZADO (anti-enumeration) — 403, never 401: 401 is reserved for an invalid session. Requires x-api-key + a valid session. Rejected for demo sessions (403 DEMO_SOLO_LECTURA).",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "additionalProperties": false,
                "properties": {
                  "email": {
                    "type": "string"
                  },
                  "nombre": {
                    "type": "string"
                  },
                  "passwordActual": {
                    "type": "string"
                  }
                },
                "type": "object"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AuthMeResponse"
                }
              }
            },
            "description": "Profile updated; the full updated identity is returned."
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PerfilErrorResponse"
                }
              }
            },
            "description": "Malformed body, or an invalid nombre/email."
          },
          "401": {
            "description": "No valid session (missing, expired, or invalid token)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PerfilErrorResponse"
                }
              }
            },
            "description": "Demo session, wrong current password, or the email is already in use."
          }
        },
        "summary": "Update the current user profile (nombre and/or email)"
      }
    },
    "/api/perfil/google/desvincular": {
      "post": {
        "description": "Authenticated endpoint (US-041, VINC041-05) that re-verifies the current password and clears the CALLER's own googleSub. Idempotent: a second call after success still responds 204. Rejected for demo sessions (403 DEMO_SOLO_LECTURA), for a wrong current password (403 PERFIL_RECHAZADO), and when the account has no passwordHash (403 VINCULO_REQUIERE_PASSWORD — CA-03: an account may never be left without an access method). Mounted unconditionally, independent of whether Google login is currently active.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "additionalProperties": false,
                "properties": {
                  "passwordActual": {
                    "type": "string"
                  }
                },
                "required": [
                  "passwordActual"
                ],
                "type": "object"
              }
            }
          }
        },
        "responses": {
          "204": {
            "description": "The Google identity is no longer linked."
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PerfilErrorResponse"
                }
              }
            },
            "description": "Malformed body."
          },
          "401": {
            "description": "No valid session (missing, expired, or invalid token)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PerfilErrorResponse"
                }
              }
            },
            "description": "Demo session, an incorrect current password, or the account has no passwordHash to fall back on."
          }
        },
        "summary": "Unlink the Google identity from the current account"
      }
    },
    "/api/perfil/google/vincular": {
      "post": {
        "description": "Authenticated endpoint (US-041, VINC041-01/02) that re-verifies the current password and starts the OIDC round trip that will bind a Google identity to the CALLER's own account — no email matching is involved. Responds with the authorization URL and sets the short-lived `md_oauth` cookie carrying an HMAC-signed link intent; the client performs a top-level navigation to that URL. Completion happens at GET /api/auth/google/callback, which redirects to `/configuracion?google=vinculado` or `/configuracion?google=error` and issues NO new session. Rejected for demo sessions (403 DEMO_SOLO_LECTURA), for a wrong current password (403 PERFIL_RECHAZADO), and when the account already carries a Google identity (409 GOOGLE_YA_VINCULADO — unlink first). 404 when Google login is not active (AUTH-16).",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "additionalProperties": false,
                "properties": {
                  "passwordActual": {
                    "type": "string"
                  }
                },
                "required": [
                  "passwordActual"
                ],
                "type": "object"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/VincularGoogleResponse"
                }
              }
            },
            "description": "The Google OAuth authorization URL to navigate to. Sets Set-Cookie: md_oauth (state, nonce, codeVerifier, signed link)."
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PerfilErrorResponse"
                }
              }
            },
            "description": "Malformed body."
          },
          "401": {
            "description": "No valid session (missing, expired, or invalid token)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PerfilErrorResponse"
                }
              }
            },
            "description": "Demo session, or an incorrect current password."
          },
          "404": {
            "description": "Google login is not active — GOOGLE_CLIENT_ID/GOOGLE_CLIENT_SECRET are not both configured (AUTH-16)."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PerfilErrorResponse"
                }
              }
            },
            "description": "The account already has a linked Google identity."
          },
          "503": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PerfilErrorResponse"
                }
              }
            },
            "description": "Google authorization is temporarily unreachable."
          }
        },
        "summary": "Start linking a Google identity to the current account"
      }
    },
    "/api/perfil/password": {
      "patch": {
        "description": "Authenticated endpoint that changes the caller's own password (US-040, PERF040-03/05/06). `passwordActual` is REQUIRED. On success, EVERY OTHER active session belonging to the caller's user is revoked — the session that made this request stays valid. A wrong `passwordActual` returns the SAME generic 403 PERFIL_RECHAZADO used by `PATCH /api/perfil` (anti-enumeration) — 403, never 401: 401 is reserved for an invalid session. `passwordNueva` must satisfy the domain password rules (8-128 characters) — an invalid one is rejected with 400 PASSWORD_INVALIDA before any write. Requires x-api-key + a valid session. Rejected for demo sessions (403 DEMO_SOLO_LECTURA).",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "additionalProperties": false,
                "properties": {
                  "passwordActual": {
                    "type": "string"
                  },
                  "passwordNueva": {
                    "type": "string"
                  }
                },
                "required": [
                  "passwordActual",
                  "passwordNueva"
                ],
                "type": "object"
              }
            }
          }
        },
        "responses": {
          "204": {
            "description": "Password changed. No response body — every other session was revoked."
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PerfilErrorResponse"
                }
              }
            },
            "description": "Malformed body, or an invalid passwordNueva."
          },
          "401": {
            "description": "No valid session (missing, expired, or invalid token)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PerfilErrorResponse"
                }
              }
            },
            "description": "Demo session, or an incorrect current password."
          }
        },
        "summary": "Change the current user's password"
      }
    },
    "/api/resumen": {
      "get": {
        "description": "Authenticated endpoint returning the 50/30/20 budget breakdown for a month (US-015/016). Requires x-api-key + a valid session (RNF-SEC-006, per-user isolation).",
        "parameters": [
          {
            "description": "Month period, format YYYY-MM (e.g. 2026-07). Absent defaults to the current month. Format is validated by the domain, not this schema.",
            "in": "query",
            "name": "periodo",
            "schema": {
              "description": "Month period, format YYYY-MM (e.g. 2026-07). Absent defaults to the current month. Format is validated by the domain, not this schema.",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ResumenMesResponse"
                }
              }
            },
            "description": "Monthly resumen for the resolved period."
          },
          "400": {
            "description": "Invalid periodo — either a transport-shape mismatch or a malformed YYYY-MM value (domain-level, PeriodoMes VO)."
          }
        },
        "summary": "Monthly 50/30/20 breakdown"
      }
    },
    "/api/resumen/anual": {
      "get": {
        "description": "Authenticated endpoint returning the 50/30/20 budget breakdown for all 12 months of a year (US-030). Requires x-api-key + a valid session (RNF-SEC-006, per-user isolation).",
        "parameters": [
          {
            "description": "Year, e.g. 2026. Absent defaults to the current year. Range/format is validated by the domain, not this schema.",
            "in": "query",
            "name": "anio",
            "schema": {
              "description": "Year, e.g. 2026. Absent defaults to the current year. Range/format is validated by the domain, not this schema.",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ResumenAnualResponse"
                }
              }
            },
            "description": "Annual resumen for the resolved year."
          },
          "400": {
            "description": "Invalid anio — either a transport-shape mismatch or a malformed/out-of-range value (domain-level)."
          }
        },
        "summary": "Annual 50/30/20 breakdown"
      }
    },
    "/api/resumen/semaforo": {
      "get": {
        "description": "Authenticated sibling detail endpoint to GET /api/resumen (US-049): exposes the semáforo classification's WHY and WHAT-TO-DO — per-bucket zone-band edges, a backend-generated Spanish diagnosis naming the driving bucket(s), and a CLP amount+direction that would return each Amarillo/Rojo bucket to Verde. Requires x-api-key + a valid session (RNF-SEC-006, per-user isolation, ISO-01/ISO-02).",
        "parameters": [
          {
            "description": "Month period, format YYYY-MM (e.g. 2026-07). Absent defaults to the current month. Format is validated by the domain, not this schema.",
            "in": "query",
            "name": "periodo",
            "schema": {
              "description": "Month period, format YYYY-MM (e.g. 2026-07). Absent defaults to the current month. Format is validated by the domain, not this schema.",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SemaforoDetalleResponse"
                }
              }
            },
            "description": "Semáforo detail for the resolved period."
          },
          "400": {
            "description": "Invalid periodo — either a transport-shape mismatch or a malformed YYYY-MM value (domain-level, PeriodoMes VO)."
          }
        },
        "summary": "Semáforo detail: zone-band edges, diagnosis, and CLP-to-Verde advice"
      }
    },
    "/api/transacciones/{id}/categoria": {
      "patch": {
        "description": "Authenticated endpoint that manually reassigns a transaction to a category (and its derived bucket) (US-013 S4). Requires x-api-key + a valid session (RNF-SEC-006, per-user isolation).",
        "parameters": [
          {
            "description": "Transaction id (raw path param). Existence/ownership is checked by the use case, not this schema.",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "Transaction id (raw path param). Existence/ownership is checked by the use case, not this schema.",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "categoriaId": {
                    "description": "Categoria id — the caller's OWN Categoria row id, resolved against their own catalog by the use case (CategoriaDesconocidaError if it does not resolve or is not theirs), not this schema.",
                    "type": "string"
                  }
                },
                "required": [
                  "categoriaId"
                ],
                "type": "object"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TransaccionesCategoriaResponse"
                }
              }
            },
            "description": "Transaction reclassified."
          },
          "400": {
            "description": "Invalid categoriaId — the given id does not resolve against the caller's own catalog, or belongs to another user (scrubbed, CategoriaDesconocidaError; ADR-037 — the closed enum gate is retired; ADR-042 — the contract identifies the categoria by id, not name)."
          },
          "404": {
            "description": "Anti-enumeration: the transaction does not exist or does not belong to the authenticated user."
          }
        },
        "summary": "Reclassify a transaction"
      }
    },
    "/api/transacciones/reevaluar": {
      "post": {
        "description": "Authenticated endpoint that re-runs CategorizarTransaccionUseCase with the caller's CURRENT pattern catalog against ALL of their persisted transactions — categorized or not, no period filter. A determined classification (a matched pattern, or the Ingreso rule) overwrites the existing categoria/bucket. A row that resolves to SinCategoria (no pattern matched) is left exactly as it is — never cleared. Rows whose determined classification already matches their current value are not re-written (transaccionesActualizadas counts real changes only). Requires x-api-key + a valid session (RNF-SEC-006, per-user isolation). Rejected for demo sessions (403 DEMO_SOLO_LECTURA).",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReevaluarCategoriasResponse"
                }
              }
            },
            "description": "Reevaluation completed."
          },
          "403": {
            "description": "The calling session is a demo session. Nothing is read or written."
          },
          "500": {
            "description": "Infrastructure fault — the caller's pattern catalog could not be loaded, or the batched write of reassignments failed (CategorizacionFallidaError). Retryable."
          }
        },
        "summary": "Re-run the caller's classification patterns over all transactions"
      }
    },
    "/version": {
      "get": {
        "description": "Public, unauthenticated endpoint exposing the currently deployed build (ADR-030).",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/VersionResponse"
                }
              }
            },
            "description": "Build info for the currently deployed API instance."
          }
        },
        "summary": "Deployed build info"
      }
    }
  }
}
